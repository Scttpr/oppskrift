# Reload the dev shell when the flake changes.
watch_file flake.nix flake.lock

# Build/enter the Nix dev shell (needs nix-direnv: https://github.com/nix-community/nix-direnv).
use flake

# Export local env vars (DATABASE_URL, JWT_SECRET, S3_*, SMTP_*, ...) for cargo/sqlx.
dotenv_if_exists .env
